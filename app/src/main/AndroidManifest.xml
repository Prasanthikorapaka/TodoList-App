<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolistapp">

    <!-- Application settings -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoListApp">

        <!-- Main activity declaration -->
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Explicitly set android:exported -->
            <!-- Intent filter to declare the main entry point of the app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Additional activity declaration for adding a new task -->
        <activity
            android:name=".AddTaskActivity"
            android:exported="false"/> <!-- Explicitly set android:exported -->

    </application>

</manifest>
